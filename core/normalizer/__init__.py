"""Normalizer Layer — unifies data from any connector into standard formats.
Every connector's raw output passes through a normalizer before reaching engines.
This decouples external APIs from internal logic.
"""

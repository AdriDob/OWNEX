"""Hermes — Automation Agent (ORION Platform app).

Safe automation for repetitive tasks: backup, health, logs, status, doctor.
Operates in safe mode by default (recommends actions, does not auto-execute).
"""

from __future__ import annotations

__version__ = "0.1.0"
